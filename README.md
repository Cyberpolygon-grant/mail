# Mail - Симуляция фишинговых атак для тестирования безопасности ДБО

Проект для симуляции фишинговых атак на систему дистанционного банковского обслуживания (ДБО). Включает почтовый сервер Mailu, генераторы фишинговых писем и скрипты для тестирования защиты от социальной инженерии.

## ⚠️ ВНИМАНИЕ

**Этот проект предназначен исключительно для образовательных целей и тестирования безопасности в контролируемой среде.** Использование этих инструментов для реальных атак является незаконным и преследуется по закону.

## 📋 Описание

Проект симулирует многоэтапную фишинговую атаку на операторов системы ДБО:

1. **Этап 1**: Компрометация оператора ДБО через фишинговое письмо с вредоносным вложением
2. **Этап 2**: Создание учетной записи злоумышленника внутри системы
3. **Этап 3**: XSS-атака для обхода валидации и создания фейковых услуг

## 🏗️ Структура проекта

```
mail/
├── docker-compose.yml          # Конфигурация Mailu и фишингового демо
├── Dockerfile                  # Образ для фишинговой атаки
├── mailu.sh                    # Скрипт управления Mailu
├── mailu.env                   # Переменные окружения Mailu
├── nginx-local.conf            # Конфигурация nginx
│
├── attack_scripts/             # Скрипты атак
│   ├── phishing_email.py      # Отправка фишинговых писем
│   ├── create_attacker_account.py  # Создание УЗ злоумышленника
│   └── xss_attack.py           # XSS-атака и обход валидации
│
├── legitimate_attachments/     # Легитимные вложения (PDF, DOCX, XLSX, ZIP)
├── malicious_attachments/      # Вредоносные вложения (XLSM с макросами)
│
├── file_generator.py           # Генератор файлов для вложений
├── generate_legitimate_files.py # Генератор легитимных файлов
└── mixed_phishing.py           # Смешанная фишинговая атака (70% легитимных, 30% вредоносных)
```

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Linux/WSL (протестировано на Kali Linux)
- Python 3.11+
- Минимум 4GB RAM

### Установка и запуск

1. **Клонирование репозитория** (если необходимо):
```bash
cd mail
```

2. **Запуск Mailu и фишинговой атаки**:
```bash
./mailu.sh start
```

3. **Создание пользователей**:
```bash
./mailu.sh users
```

4. **Просмотр логов атаки**:
```bash
./mailu.sh logs
```

5. **Проверка статуса**:
```bash
./mailu.sh status
```

### Доступ к системе

После запуска доступны следующие интерфейсы:

- **Веб-почта (Roundcube)**: http://financepro.ru/webmail
- **Админ-панель Mailu**: http://financepro.ru/admin
- **SMTP**: `front:25` (внутри Docker сети)

### Учетные данные

После выполнения `./mailu.sh users` создается:

- **operator1@financepro.ru** / `1q2w#E$R` - Оператор ДБО #1 (жертва фишинга)

## 📧 Типы писем

### Легитимные письма (70%)

Легитимные письма имитируют реальные запросы клиентов банка:

- **Темы**: "Создание учетной записи в системе ДБО", "Запрос на увеличение лимитов", "Документы для регистрации в ДБО" и др.
- **Отправители**: Легитимные домены банков (sberbank.ru, vtb.ru, alfabank.ru и др.)
- **Вложения**: PDF, DOCX, XLSX, ZIP файлы с правдоподобным содержимым
- **Содержимое**: Профессиональные запросы без спам-маркеров

### Вредоносные письма (30%)

Вредоносные письма содержат признаки фишинга:

- **Темы**: С добавлением срочности ("- требуется срочно", "- важно!", "- просим ускорить")
- **Отправители**: Поддельные домены (sber-bank.ru, vtb-bank.ru, sberbankk.ru и др.)
- **Вложения**: XLSM файлы с VBA макросами для кражи данных
- **Содержимое**: Навязчивость, повторения, эмоциональные маркеры

## 🔧 Конфигурация

### Переменные окружения

В `docker-compose.yml` можно настроить:

```yaml
environment:
  - TARGET_EMAIL=operator1@financepro.ru  # Целевой получатель
  - SMTP_SERVER=front                     # SMTP сервер
  - SMTP_PORT=25                          # SMTP порт
```

### Настройка Mailu

Основные настройки в `mailu.env`:

- `DOMAIN=financepro.ru` - Домен почтового сервера
- `SECRET_KEY` - Секретный ключ для шифрования
- `HOSTNAME=mail.financepro.ru` - Имя хоста

### Генерация файлов

Для генерации легитимных файлов:

```bash
python3 generate_legitimate_files.py
```

Скрипт создаст файлы в `legitimate_attachments/`:
- PDF документы (заявления, справки, доверенности)
- XLSX таблицы (регистрационные формы, списки пользователей)
- DOCX документы (запросы, справки)
- ZIP архивы (пакеты документов)

## 📝 Использование скриптов

### Смешанная фишинговая атака

Основной скрипт для симуляции атаки:

```bash
python3 mixed_phishing.py
```

Отправляет письма каждые 5 секунд с соотношением 70% легитимных / 30% вредоносных.

### Отдельные этапы атаки

**Этап 1 - Фишинговое письмо**:
```bash
python3 attack_scripts/phishing_email.py
```

**Этап 2 - Создание УЗ злоумышленника**:
```bash
python3 attack_scripts/create_attacker_account.py
```

**Этап 3 - XSS атака**:
```bash
python3 attack_scripts/xss_attack.py
```

### Генерация файлов

**Легитимные файлы**:
```bash
python3 generate_legitimate_files.py
```

**Вложения для писем** (используется автоматически):
```python
from file_generator import create_file_attachment

# Легитимный файл
content, filename, mime_type = create_file_attachment(
    "pdf", "ООО Компания", is_malicious=False, 
    subject="Создание учетной записи в системе ДБО"
)

# Вредоносный файл
content, filename, mime_type = create_file_attachment(
    "excel", "ООО Компания", is_malicious=True
)
```

## 🛡️ Защита от фишинга

Проект демонстрирует различные техники фишинга:

1. **Спуфинг доменов**: Использование похожих доменов (sber-bank.ru вместо sberbank.ru)
2. **Социальная инженерия**: Создание срочности и важности
3. **Вредоносные вложения**: Excel файлы с VBA макросами
4. **Смешанные атаки**: Легитимные письма для снижения подозрений
5. **XSS атаки**: Обход валидации через внедрение скриптов

### Рекомендации по защите

- Обучение сотрудников распознаванию фишинга
- Использование SPF, DKIM, DMARC для проверки отправителей
- Сканирование вложений антивирусом
- Блокировка выполнения макросов в Office документах
- Валидация и санитизация пользовательского ввода (защита от XSS)
- Мониторинг подозрительной активности

## 📊 Мониторинг

### Логи фишинговой атаки

```bash
./mailu.sh logs
```

или напрямую:

```bash
docker compose logs phishing-demo
```

### Логи Mailu

```bash
docker compose logs front
docker compose logs smtp
docker compose logs antispam
```

### Статус сервисов

```bash
./mailu.sh status
```

## 🧹 Очистка

### Остановка сервисов

```bash
./mailu.sh stop
```

### Полная очистка

```bash
./mailu.sh clean
```

⚠️ **Внимание**: Это удалит все данные Mailu, включая письма и пользователей!

## 🔍 Архитектура

### Компоненты Mailu

- **front** (nginx): Веб-интерфейс и прокси
- **admin**: Админ-панель и управление пользователями
- **smtp** (postfix): Отправка почты
- **imap** (dovecot): Получение почты
- **antispam** (rspamd): Фильтрация спама
- **webmail** (roundcube): Веб-интерфейс почты
- **resolver** (dnsmasq): Локальный DNS резолвер

### Фишинговый демо

- **phishing-demo**: Контейнер с Python скриптами для отправки фишинговых писем

## 🐛 Устранение неполадок

### SMTP сервер не готов

Если видите ошибки подключения к SMTP:

1. Проверьте статус сервисов: `./mailu.sh status`
2. Дождитесь полного запуска (может занять 1-2 минуты)
3. Проверьте логи: `docker compose logs smtp`

### Письма не доставляются

1. Проверьте настройки DNS в `docker-compose.yml`
2. Убедитесь, что домен `financepro.ru` разрешается локально
3. Проверьте логи antispam: `docker compose logs antispam`

### Ошибки при генерации файлов

1. Убедитесь, что папки `legitimate_attachments/` и `malicious_attachments/` существуют
2. Запустите генератор: `python3 generate_legitimate_files.py`

## 📚 Дополнительная информация

### Технологии

- **Mailu**: Полнофункциональный почтовый сервер
- **Python 3.11**: Скрипты для генерации и отправки писем
- **Docker Compose**: Оркестрация контейнеров
- **Rspamd**: Антиспам фильтрация
- **Roundcube**: Веб-интерфейс почты

### Безопасность

- Все компоненты работают в изолированных Docker контейнерах
- Используется локальная сеть для коммуникации
- DNS резолвер настроен для локального домена
- Антивирус отключен для упрощения демонстрации

## 📄 Лицензия

Этот проект предназначен исключительно для образовательных целей и тестирования безопасности.

## 🤝 Вклад

При использовании этого проекта для обучения или тестирования безопасности, убедитесь, что:

1. Вы имеете право на тестирование целевой системы
2. Тестирование проводится в контролируемой среде
3. Результаты используются для улучшения безопасности
4. Соблюдаются все применимые законы и правила

## ⚠️ Отказ от ответственности

Авторы проекта не несут ответственности за неправомерное использование этих инструментов. Использование для реальных атак является незаконным и может повлечь уголовную ответственность.
